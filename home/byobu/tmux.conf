new -A -s ethminer -n htop htop;
neww -n mine tail --follow=name $HOME/local0.log $HOME/local1.log $HOME/local2.log ;
splitw -v -p 40 -t 0 watch -n 5 "nvidia-smi; echo; nvidia-smi --format=csv --query-gpu=name,power.draw,fan.speed,temperature.gpu,clocks.video,clocks.mem";
# selectw -t 0;
# selectp -t 0;
